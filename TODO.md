# لیست جامع دارایی‌های صوتی (Audio Assets) جهت ضبط یا TTS

این سند شامل تمامی فایل‌های صوتی مورد نیاز پروژه **چیستان‌آباد** است. برای بهترین خروجی، صداها باید با لحنی آرام، مهربان و تشویقی ضبط شوند.

**تنظیمات پیشنهادی برای خروجی TTS:**
- **زبان:** فارسی (ایران)
- **سرعت:** ۰.۹ (کمی آهسته‌تر از نرمال)

---

## ۱. راهنماهای صوتی آواتار (Avatar Instructions)
این فایل‌ها در پوشه `res/raw` قرار می‌گیرند یا توسط TTS پخش می‌شوند.

| نام فایل (ID) | متن دقیق جهت ضبط / ورودی TTS | کاربرد |
| :--- | :--- | :--- |
| `inst_type_word` | "قهرمان من، بیا این کلمه رو با هم بنویسیم!" | شروع هر مرحله آموزشی |
| `inst_find_char` | "می‌تونی حرفی که دنبالشیم رو روی دکمه‌ها پیدا کنی؟" | راهنمای صوتی هنگام مکث کاربر (Hint) |
| `inst_category` | "کدوم جزیره رو برای ماجراجویی امروز انتخاب می‌کنی؟" | صفحه انتخاب بین الفبا و اعداد |
| `inst_review` | "وقتشه بریم شهربازی و چیزهایی که یاد گرفتیم رو مرور کنیم!" | ورود به بخش مرور (Saga Map) |

---

## ۲. صدای حروف الفبا (Alphabet Phonics)
برای هر حرف، فایل صوتی شامل نام حرف و صدای کشیده آن است.

| نام فایل | متن (نام + صدای حرف) | نام فایل | متن (نام + صدای حرف) |
| :--- | :--- | :--- | :--- |
| `audio_a1` | "بـ ... مثل آب" | `audio_a17` | "قـ ... مثل قایق" |
| `audio_a2` | "آ ... مثل بابا" | `audio_a3` | "د ... مثل باد" |
| `audio_a4` | "مـ ... مثل بام" | `audio_a5` | "سـ ... مثل سبد" |
| `audio_a6` | "نـ ... مثل نان" | `audio_a7` | "ر ... مثل ابر" |
| `audio_a8` | "تـ ... مثل دست" | `audio_a9` | "و ... مثل بوم" |
| `audio_a10` | "یـ ... مثل سیب" | `audio_a11` | "ز ... مثل باز" |
| `audio_a12` | "شـ ... مثل آش" | `audio_a13` | "کـ ... مثل کتاب" |
| `audio_a14` | "گـ ... مثل سگ" | `audio_a15` | "فـ ... مثل برف" |
| `audio_a16` | "خـ ... مثل شاخ" | `audio_a18` | "لـ ... مثل لباس" |
| `audio_a19` | "جـ ... مثل تاج" | `audio_a20` | "چـ ... مثل چای" |
| `audio_a21` | "هـ ... مثل کوه" | `audio_a22` | "ژ ... مثل ژله" |
| `audio_a23` | "صـ ... مثل صورت" | `audio_a24` | "ذ ... مثل ذرت" |
| `audio_a25` | "عـ ... مثل عینک" | `audio_a26` | "ث ... مثل ثروت" |
| `audio_a27` | "ح ... مثل حلزون" | `audio_a28` | "ض ... مثل ضامن" |
| `audio_a29` | "ط ... مثل طوطی" | `audio_a30` | "غ ... مثل غذا" |
| `audio_a31` | "ظ ... مثل ظرف" | | |

---

## ۳. صدای اعداد (Number Audio)

| نام فایل | متن (تلفظ عدد) | نام فایل | متن (تلفظ عدد) |
| :--- | :--- | :--- | :--- |
| `audio_n0` | "صفر" | `audio_n5` | "پنج" |
| `audio_n1` | "یک" | `audio_n6` | "شش" |
| `audio_n2` | "دو" | `audio_n7` | "هفت" |
| `audio_n3` | "سه" | `audio_n8` | "هشت" |
| `audio_n4` | "چهار" | `audio_n9` | "نه" |

---

## ۴. استراتژی قرائت متن (TTS Strategy) - کاملاً نیتیو و آفلاین
برای اینکه اپلیکیشن بتواند بدون اینترنت و به صورت نیتیو کلمات را به فارسی (گویش ایرانی) بخواند، موارد زیر الزامی است:

1. **نصب داده‌های صوتی (Voice Data):**
   - کاربر باید در تنظیمات گوشی به مسیر زیر برود:
     `Settings > Languages & Input > Text-to-speech output`
   - موتور `Google Services` را انتخاب کرده و در بخش `Install voice data` زبان `Persian (Iran)` را دانلود کند.
   
2. **الزامات پیاده‌سازی در کد:**
   - کلاس `TtsManager` از سیستم نیتیو اندروید استفاده می‌کند.
   - از متد `openTtsSettings()` برای راهنمایی کاربر جهت نصب دیتای آفلاین استفاده می‌شود.
   - در صورت عدم دسترسی به دیتای محلی، از موتور آنلاین گوگل به عنوان Fallback استفاده می‌شود.

3. **مزیت این روش:**
   - حذف تمام وابستگی‌های شخص ثالث و سنگین (مانند ONNX/Sherpa).
   - حفظ انعطاف‌پذیری کامل برای قرائت هر متن داینامیک.
   - کاهش قابل توجه حجم نهایی فایل نصب (APK).
   - پایداری بالا و هماهنگی با سیستم عامل اندروید.

---
**نکته نهایی:** تمامی نام فایل‌ها باید با حروف کوچک انگلیسی باشند و هیچ فاصله یا کاراکتر خاصی نداشته باشند.
