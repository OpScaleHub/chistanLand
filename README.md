# 🏝️ چستان‌لند (ChistanLand) - پلتفرم ماجراجویی آموزشی کودکان

**چستان‌لند** یک اپلیکیشن آموزشی نوین برای کودکان پیش‌دبستانی است که یادگیری حروف الفبا و اعداد فارسی را از یک فرآیند تکراری به یک «سفر روایی» در جزایر دانایی تبدیل کرده است. این پروژه با الهام از اصول روانشناسی رشد و متدهای مدرن تعامل (UX)، تجربه‌ای بدون نیاز به متن (Audio-First) را فراهم می‌کند.

---

## 🏗️ معماری و استراتژی طراحی

### ۱. استعاره بصری (The Island Metaphor)
برخلاف مدل‌های سنتی، در اینجا پیشرفت کودک به صورت بصری روی یک **نقشه سفر (Saga Map)** نمایش داده می‌شود.
- **جزایر دانایی:** ایستگاه‌های یادگیری حروف الفبا.
- **قله اعداد:** مسیر صعودی برای یادگیری اعداد.
- **شهربازی مرور:** ایستگاه‌های ویژه برای تثبیت حافظه بلندمدت که پس از هر ۳ موفقیت فعال می‌شوند.

### ۲. سیستم تایپ هوشمند (Kid-Friendly Keyboard)
با الهام از کانسپت *MonkeyType Junior*، مانع فیزیکی تایپ برای کودک حذف شده است:
- **کیبورد انطباقی:** تنها حروف مورد نیاز به همراه چند مزاحم (Distractors) نمایش داده می‌شوند.
- **درخشش راهنما (Target Glow):** در صورت مکث کودک، حرف صحیح به صورت ملایم می‌درخشد.
- **بازخورد لرزشی (Haptic Feedback):** استفاده از لرزش و انیمیشن Shake برای اعلام خطا بدون ایجاد حس شکست.

### ۳. موتور یادگیری تطبیقی (Adaptive Leitner)
سیستم تکرار فاصله‌دار (Spaced Repetition) در لایه‌ی دیتا مدیریت می‌شود:
- **سطوح رشد:** وضعیت هر حرف از «نوپا» تا «استاد» تغییر می‌کند که با نمادهای بصری (رشد گیاه و وضعیت جوجه) نمایش داده می‌شود.

---

## 🛠️ جزئیات فنی و پیاده‌سازی

- **زبان و فریم‌ورک:** Kotlin & Jetpack Compose.
- **پایگاه داده:** Room Persistence برای مدیریت وضعیت یادگیری به صورت Offline-First.
- **مدیریت وضعیت (State):** MVVM Pattern همراه با کاتلین Flow و SharedFlow برای رویدادهای یک‌باره (مانند لرزش خطا).
- **انیمیشن‌ها:** استفاده گسترده از `Animatable` و Lottie برای بازخوردهای روان.
- **CI/CD:** پایگاه داده و بیلد بهینه شده با GitHub Actions همراه با سیستم کشینگ پیشرفته.

---

## 🎙️ راهنمای تولید محتوا (Asset Specification)

برای عملکرد کامل برنامه، دارایی‌های زیر باید در مسیرهای مشخص قرار گیرند:

### ۱. محتوای صوتی (Phonics)
تمامی فایل‌ها باید با فرمت `.mp3` در `app/src/main/res/raw/` قرار گیرند.
- **نام‌گذاری حروف:** `audio_a1` (آ) تا `audio_a33` (ی).
- **نام‌گذاری اعداد:** `audio_n0` (۰) تا `audio_n9` (۹).
- **افکت‌ها:** `pop_sound` (تایید)، `error_sound` (خطا)، `carnival_music` (شروع شهربازی).

**الگوی ضبط:** [نام حرف] + (۱ ثانیه مکث) + "مثلِ" + (۰.۵ ثانیه مکث) + [کلمه مثال].
*مثال: "ب... مثلِ... باران"*

### ۲. محتوای بصری (Illustrations)
تصاویر SVG یا PNG در `app/src/main/res/drawable/`:
- اسامی مطابق دیتابیس: `img_a1`, `img_a2`, ...

---

## 🚀 نقشه راه و وضعیت نهایی

- [x] پیاده‌سازی کامل دیتابیس لایتنر و منطق پیشرفت.
- [x] طراحی نقشه سفر تعاملی و قفل‌های منطقی.
- [x] داشبورد والدین با روایت‌های انسانی (Generative UI).
- [x] سیستم کشینگ و بهینه‌سازی بیلد در GitHub Actions.
- [ ] یکپارچه‌سازی نهایی فایل‌های صوتی با کیفیت استودیویی.
- [ ] اضافه کردن آواتار راهنما برای دستورالعمل‌های صوتی زنده.

---
*توسعه یافته برای آینده‌ای روشن‌تر برای کودکان ایران زمین.*
