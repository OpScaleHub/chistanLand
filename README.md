# 🏝️ چیستان‌آباد (ChistanLand) - پلتفرم ماجراجویی آموزشی کودکان

**چیستان‌آباد** یک پلتفرم آموزشی نوین بر پایه سیستم تکرار فاصله‌دار (Spaced Repetition) و معماری اقلیم‌های یادگیری است.

---

## 🏗️ معماری آموزشی (Pedagogical Architecture)

### ۱. سیستم اقلیم‌های ۵گانه (Learning Biomes)
یادگیری از یک مسیر خطی به یک سفر اکتشافی تبدیل شده است. هر نشانه (حرف یا عدد) باید از ۵ اقلیم عبور کند تا به سطح «تسلط» (Mastery) برسد:

1.  **🏝️ جزیره آواها (Phonics Intro):** آشنایی با صدا و شکل. تعامل ساده (یک کلیک).
2.  **⛰️ دره نشانه‌ها (Missing Letter):** تشخیص جایگاه حرف در کلمه. تقویت حافظه دیداری.
3.  **🌲 جنگل واژه‌ها (Drag & Drop):** ترکیب حروف و ساخت کلمه. تقویت هماهنگی چشم و دست.
4.  **🏰 قلعه کلمات (Word Recognition):** تشخیص کلمه بین گزینه‌ها و تایپ با حروف مزاحم.
5.  **🏔️ کوهستان تسلط (Quick Recall):** مرور سریع و تثبیت در حافظه بلندمدت.

### ۲. پیشرفت تدریجی (XP System)
کودک برای عبور از هر اقلیم به سطح بعدی، نیاز به کسب **۳ امتیاز تجربه (XP)** در آن اقلیم دارد. این کار باعث تکرار هدفمند و عمیق‌تر شدن یادگیری می‌شود.

### ۳. قانون یادگیری تجمعی (Cumulative Law)
*   **قانون ۱+ :** در هر مرحله فقط یک نشانه جدید معرفی می‌شود.
*   **پایداری محتوا:** کلمات هر مرحله فقط از حروفی ساخته می‌شوند که کودک قبلاً آن‌ها را آموخته است.

---

## 🛠️ جزئیات فنی و پیاده‌سازی

### تعاملات نوین (Modern Interactions)
*   **Drag & Drop:** در اقلیم ۳، کودک باید حروف را به خانه‌های خالی بکشد.
*   **کیبورد تطبیقی:** تعداد حروف مزاحم بر اساس اقلیم فعلی تغییر می‌کند (از ۰ تا ۸ حرف مزاحم).
*   **پشتیبانی آفلاین:** تمام صوت‌ها (TTS محلی) و تصاویر کاملاً آفلاین هستند.

### شناسه‌های محتوا (Content IDs)
*   الفبا: `a1` (آ، ب) تا `a31` (ظ).
*   اعداد: `n1` تا `n0`.
*   تصاویر: پیشوند `img_` + شناسه.
*   صوت‌ها: پیشوند `audio_` + شناسه.

---

## 📋 دستورالعمل توسعه آتی
*   **توسعه بازی‌های جدید:** هر بازی جدید باید در کلاس `ActivityType` تعریف و در `decideActivityType` به اقلیم مربوطه اختصاص یابد.
*   **سیستم پاداش:** در آینده، کسب Mastery در هر اقلیم باید یک «دارایی بصری» (مثل یک حیوان یا درخت) به نقشه اصلی اضافه کند.
*   **پایداری:** همیشه از `toPersianDigit()` برای نمایش اعداد استفاده شود.
